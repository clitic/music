name: deploy

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cargo install https://github.com/clitic/trending-music.git
      - run: ./trending-music
      - run: ls
